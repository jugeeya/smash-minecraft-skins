name: Rust

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  plugin:
    env:
      PLUGIN_NAME: minecraft_skins
    runs-on: ubuntu-latest
    container: 
      image: devkitpro/devkita64:latest
    steps:
    - uses: actions/checkout@v2
    - name: install
      run: |
        curl https://sh.rustup.rs -sSf > /tmp/rustup-init.sh     && chmod +x /tmp/rustup-init.sh     && sh /tmp/rustup-init.sh -y     && rm -rf /tmp/rustup-init.sh # buildkit
    - name: install 2
      run: |
        apt update &&  apt install -y build-essential libssl-dev &&  ~/.cargo/bin/rustup install stable && ~/.cargo/bin/cargo install cargo-skyline && cargo skyline update-std
    - name: Build release NRO
      run: |
        PATH=$PATH:/root/.cargo/bin:/opt/devkitpro/tools/bin  RUST_BACKTRACE=1 /root/.cargo/bin/cargo-skyline skyline build --release
      env:
        HOME: /root
    - name: Upload plugin artifact
      uses: actions/upload-artifact@v2
      with:
        name: plugin
        path: target/aarch64-skyline-switch/release/lib${{env.PLUGIN_NAME}}.nro
#   upload:
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main'
#     needs:
#       - plugin
#     steps:
#     - name: Download all artifacts
#       uses: actions/download-artifact@v2
#     - name: Update Release
#       uses: meeDamian/github-release@2.0
#       with:
#         token: ${{ secrets.GITHUB_TOKEN }}
#         prerelease: true
#         allow_override: true
#         gzip: false
#         tag: beta
#         name: beta
#         body: >
#           Beta built off of the latest code in the repository. 
#         files: >
#           plugin/* 
